{
  "name": "database",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "module": "./dist/index.mjs",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "studio": "prisma studio",
    "build": "tsup index.ts --dts --format cjs",
    "start": "tsup index.ts --dts --format cjs",
    "dev": "tsup index.ts --dts --format cjs"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "tsup": "^8.0.2"
  },
  "devDependencies": {
    "prisma": "latest"
  }
}